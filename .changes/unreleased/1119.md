## [Unreleased] — #1119: reconcile role-baton-routing with v1.1 taxonomy

### Changed
- `instructions/role-baton-routing.instructions.md` — reconcile Status Workflow + Transition Guards with current 10-state taxonomy v1.1 (`instructions/ticket-driven-work.instructions.md`). Replaces stale 7-state model (`backlog/todo/in-progress/testing/review/done/cancelled`) with current 10-state (`backlog/triage/ready/in-progress/testing/review/done/cancelled/dormant/deferred`). Removes typed-collaborator suffix `role:collab-{type}` in favor of `role:collaborator` + area labels. Updates board name from "DevEnv Ops Board" to "Megingjord Harness Board". Per #1105 D-008 (Copilot Team CP-RD HIGH-severity finding).
