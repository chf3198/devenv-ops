## [Unreleased] — #1208: Workflow self-anneal Phase-B delivery

### Added
- `.github/workflows/anneal-nightly.yml` schedules nightly anneal detection (`0 6 * * *`) and posts findings to Epic #1133.
- `scripts/global/anneal-goal-sensor.js` computes `recurring_failure_rate` over a 7-day window and writes `generated/anneal-sensor.json`.
- `scripts/global/anneal-graph.js` builds Mermaid pattern co-occurrence output in `research/anneal-pattern-graph.md`.
- `tests/anneal-detect.test.js` and `tests/fixtures/mock-gh-api.js` add synthetic CI-safe coverage for hit/miss/empty/corrupt inputs.

### Verification
- `node scripts/global/anneal-goal-sensor.js`
- `node scripts/global/anneal-graph.js`
- `node --test tests/anneal-detect.test.js`
